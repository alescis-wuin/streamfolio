streamfolio:
  media:
    storage: local
    root: ${STREAMFOLIO_MEDIA_ROOT:./data/media}
  security:
    cookie-secure: ${STREAMFOLIO_COOKIE_SECURE:false}
  minio:
    enabled: ${STREAMFOLIO_MINIO_ENABLED:false}
